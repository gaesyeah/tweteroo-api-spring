services:
  db:
    image: postgres:16
    container_name: tweteroo-db
    environment:
      POSTGRES_DB: tweteroo
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - tweteroo-data:/var/lib/postgresql/data

volumes:
  tweteroo-data:
